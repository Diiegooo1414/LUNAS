/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./*.{html,js}"], // <--- ESTO ES LO IMPORTANTE (Busca en la raíz)
    theme: {
        extend: {},
    },
    plugins: [],
}